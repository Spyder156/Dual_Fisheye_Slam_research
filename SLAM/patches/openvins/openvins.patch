diff --git a/ov_core/src/track/Grider_GRID.h b/ov_core/src/track/Grider_GRID.h
index 59cede9..60944c3 100644
--- a/ov_core/src/track/Grider_GRID.h
+++ b/ov_core/src/track/Grider_GRID.h
@@ -130,7 +130,13 @@ public:
                       // Append the "best" ones to our vector
                       // Note that we need to "correct" the point u,v since we extracted it in a ROI
                       // So we should append the location of that ROI in the image
-                      for (size_t i = 0; i < (size_t)num_features_grid && i < pts_new.size(); i++) {
+                      // project-local fix: upstream truncated to the top num_features_grid
+                      // by response and only THEN applied the mask. The strongest corners in a
+                      // cell usually sit on already-tracked features (which the mask excludes),
+                      // so every candidate got rejected and the cell yielded nothing. Filter
+                      // first, then keep the best num_features_grid survivors.
+                      size_t kept = 0;
+                      for (size_t i = 0; i < pts_new.size() && kept < (size_t)num_features_grid; i++) {
 
                         // Create keypoint
                         cv::KeyPoint pt_cor = pts_new.at(i);
@@ -146,6 +152,7 @@ public:
                         if (mask.at<uint8_t>((int)pt_cor.pt.y, (int)pt_cor.pt.x) > 127)
                           continue;
                         collection.at(r).push_back(pt_cor);
+                        kept++;
                       }
                     }
                   }));
diff --git a/ov_core/src/track/TrackKLT.cpp b/ov_core/src/track/TrackKLT.cpp
index d2fe388..5fe47ec 100644
--- a/ov_core/src/track/TrackKLT.cpp
+++ b/ov_core/src/track/TrackKLT.cpp
@@ -31,6 +31,14 @@
 
 using namespace ov_core;
 
+namespace ov_core {
+// project-local rolling-shutter compensation: readout time (s, signed for scan
+// direction) and per-camera angular velocity (camera frame) at the current frame.
+double g_rs_readout_s = 0.0;
+Eigen::Vector3d g_rs_w_cam[4] = {Eigen::Vector3d::Zero(), Eigen::Vector3d::Zero(), Eigen::Vector3d::Zero(), Eigen::Vector3d::Zero()};
+} // namespace ov_core
+
+
 void TrackKLT::feed_new_camera(const CameraData &message) {
 
   // Error check that we have all the data
@@ -172,10 +180,38 @@ void TrackKLT::feed_monocular(const CameraData &message, size_t msg_id) {
     }
   }
 
+  PRINT_INFO("[POOLSTATS] cam=%zu pool_pre=%d after_det=%zu tracked_good=%zu\n", cam_id, pts_before_detect, pts_left_old.size(),
+             good_left.size());
+
   // Update our feature database, with theses new observations
+  // Rolling-shutter compensation: rotate each feature's bearing by w*dt where
+  // dt is the row's readout offset from the frame-center timestamp.
+  const double rs = g_rs_readout_s;
+  const Eigen::Vector3d w_c = (cam_id < 4) ? g_rs_w_cam[cam_id] : Eigen::Vector3d::Zero();
   for (size_t i = 0; i < good_left.size(); i++) {
-    cv::Point2f npt_l = camera_calib.at(cam_id)->undistort_cv(good_left.at(i).pt);
-    database->update_feature(good_ids_left.at(i), message.timestamp, cam_id, good_left.at(i).pt.x, good_left.at(i).pt.y, npt_l.x, npt_l.y);
+    cv::Point2f uv = good_left.at(i).pt;
+    if (rs != 0.0 && w_c.norm() > 1e-6) {
+      double dt = (uv.y / (double)img.rows - 0.5) * rs;
+      Eigen::Vector2f zn = camera_calib.at(cam_id)->undistort_f(Eigen::Vector2f(uv.x, uv.y));
+      Eigen::Vector3d b(zn(0), zn(1), 1.0);
+      b.normalize();
+      Eigen::Vector3d th = w_c * dt;
+      double a = th.norm();
+      if (a > 1e-12) {
+        Eigen::Vector3d k = th / a;
+        Eigen::Matrix3d K;
+        K << 0, -k(2), k(1), k(2), 0, -k(0), -k(1), k(0), 0;
+        Eigen::Matrix3d R = Eigen::Matrix3d::Identity() + std::sin(a) * K + (1 - std::cos(a)) * K * K;
+        b = R * b;
+      }
+      if (b(2) > 1e-6) {
+        Eigen::Vector2f uv2 = camera_calib.at(cam_id)->distort_f(Eigen::Vector2f(b(0) / b(2), b(1) / b(2)));
+        if (uv2(0) >= 0 && uv2(0) < img.cols && uv2(1) >= 0 && uv2(1) < img.rows)
+          uv = cv::Point2f(uv2(0), uv2(1));
+      }
+    }
+    cv::Point2f npt_l = camera_calib.at(cam_id)->undistort_cv(uv);
+    database->update_feature(good_ids_left.at(i), message.timestamp, cam_id, uv.x, uv.y, npt_l.x, npt_l.y);
   }
 
   // Move forward in time
@@ -465,7 +501,10 @@ void TrackKLT::perform_detection_monocular(const std::vector<cv::Mat> &img0pyr,
 
   // First compute how many more features we need to extract from this image
   // If we don't need any features, just return
-  double min_feat_percent = 0.50;
+  static double min_feat_percent = []() {
+    const char *e = std::getenv("OV_MIN_FEAT_PERCENT");
+    return (e != nullptr) ? std::atof(e) : 0.50;
+  }();
   int num_featsneeded = num_features - (int)pts0.size();
   if (num_featsneeded < std::min(20, (int)(min_feat_percent * num_features)))
     return;
@@ -492,6 +531,8 @@ void TrackKLT::perform_detection_monocular(const std::vector<cv::Mat> &img0pyr,
   }
   std::vector<cv::KeyPoint> pts0_ext;
   Grider_GRID::perform_griding(img0pyr.at(0), mask0_updated, valid_locs, pts0_ext, num_features, grid_x, grid_y, threshold, true);
+  PRINT_INFO("[DETDBG] imgsz=%dx%d need=%d validcells=%zu/%d ext=%zu thresh=%d numfeat=%d\n", img0pyr.at(0).cols,
+             img0pyr.at(0).rows, num_featsneeded, valid_locs.size(), grid_x * grid_y, pts0_ext.size(), threshold, num_features);
 
   // Now, reject features that are close a current feature
   std::vector<cv::KeyPoint> kpts0_new;
@@ -600,7 +641,10 @@ void TrackKLT::perform_detection_stereo(const std::vector<cv::Mat> &img0pyr, con
   }
 
   // First compute how many more features we need to extract from this image
-  double min_feat_percent = 0.50;
+  static double min_feat_percent = []() {
+    const char *e = std::getenv("OV_MIN_FEAT_PERCENT");
+    return (e != nullptr) ? std::atof(e) : 0.50;
+  }();
   int num_featsneeded_0 = num_features - (int)pts0.size();
 
   // LEFT: if we need features we should extract them in the current frame
@@ -870,7 +914,25 @@ void TrackKLT::perform_matching(const std::vector<cv::Mat> &img0pyr, const std::
   double max_focallength_img0 = std::max(camera_calib.at(id0)->get_K()(0, 0), camera_calib.at(id0)->get_K()(1, 1));
   double max_focallength_img1 = std::max(camera_calib.at(id1)->get_K()(0, 0), camera_calib.at(id1)->get_K()(1, 1));
   double max_focallength = std::max(max_focallength_img0, max_focallength_img1);
-  cv::findFundamentalMat(pts0_n, pts1_n, cv::FM_RANSAC, 2.0 / max_focallength, 0.999, mask_rsc);
+  // project-local: wide-FOV cameras amplify pixel noise in normalized coords near the
+  // image edge, so the fixed 2px-equivalent threshold executes valid edge features.
+  // OV_RANSAC_PX overrides the pixel-equivalent threshold (default 2.0 = upstream).
+  static double ransac_px = []() {
+    const char *e = std::getenv("OV_RANSAC_PX");
+    return (e != nullptr) ? std::atof(e) : 2.0;
+  }();
+  cv::findFundamentalMat(pts0_n, pts1_n, cv::FM_RANSAC, ransac_px / max_focallength, 0.999, mask_rsc);
+  {
+    int klt_ok = 0, rsc_rej = 0;
+    for (size_t i = 0; i < mask_klt.size(); i++) {
+      if (mask_klt.at(i)) {
+        klt_ok++;
+        if (!mask_rsc.at(i))
+          rsc_rej++;
+      }
+    }
+    PRINT_INFO("[KLTSTATS] cam=%zu klt_ok=%d rsc_rej=%d\n", id0, klt_ok, rsc_rej);
+  }
 
   // Loop through and record only ones that are valid
   for (size_t i = 0; i < mask_klt.size(); i++) {
diff --git a/ov_init/src/init/InertialInitializerOptions.h b/ov_init/src/init/InertialInitializerOptions.h
index 47d1c3b..49c9a84 100644
--- a/ov_init/src/init/InertialInitializerOptions.h
+++ b/ov_init/src/init/InertialInitializerOptions.h
@@ -29,6 +29,7 @@
 #include <vector>
 
 #include "cam/CamEqui.h"
+#include "cam/CamMei.h"
 #include "cam/CamRadtan.h"
 #include "feat/FeatureInitializerOptions.h"
 #include "track/TrackBase.h"
@@ -304,7 +305,12 @@ struct InertialInitializerOptions {
         cam_eigen.block(4, 0, 3, 1) = -T_CtoI.block(0, 0, 3, 3).transpose() * T_CtoI.block(0, 3, 3, 1);
 
         // Create intrinsics model
-        if (dist_model == "equidistant") {
+        if (dist_model == "mei") {
+          double cam_xi = 0.0;
+          parser->parse_external("relative_config_imucam", "cam" + std::to_string(i), "xi", cam_xi);
+          camera_intrinsics.insert({i, std::make_shared<ov_core::CamMei>(matrix_wh.at(0), matrix_wh.at(1), cam_xi)});
+          camera_intrinsics.at(i)->set_value(cam_calib);
+        } else if (dist_model == "equidistant") {
           camera_intrinsics.insert({i, std::make_shared<ov_core::CamEqui>(matrix_wh.at(0), matrix_wh.at(1))});
           camera_intrinsics.at(i)->set_value(cam_calib);
         } else {
diff --git a/ov_msckf/cmake/ROS1.cmake b/ov_msckf/cmake/ROS1.cmake
index 2ea4292..9ccfcfc 100644
--- a/ov_msckf/cmake/ROS1.cmake
+++ b/ov_msckf/cmake/ROS1.cmake
@@ -158,3 +158,9 @@ install(TARGETS test_sim_repeat
         RUNTIME DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
 )
 
+
+##################################################
+# project-local: ROS-free folder-dataset runner
+##################################################
+add_executable(run_folder src/run_folder.cpp)
+target_link_libraries(run_folder ov_msckf_lib ${thirdparty_libraries})
diff --git a/ov_msckf/src/core/VioManager.cpp b/ov_msckf/src/core/VioManager.cpp
index 6a0d7b9..5757399 100644
--- a/ov_msckf/src/core/VioManager.cpp
+++ b/ov_msckf/src/core/VioManager.cpp
@@ -503,6 +503,10 @@ void VioManager::do_feature_propagate_update(const ov_core::CameraData &message)
   // Now that we have a list of features, lets do the EKF update for MSCKF and SLAM!
   //===================================================================================
 
+  PRINT_INFO("[FSTATS] t=%.3f lost=%zu marg=%zu maxtracks=%zu slam_up=%zu slam_new=%zu msckf_in=%zu\n", message.timestamp,
+             feats_lost.size(), feats_marg.size(), feats_maxtracks.size(), feats_slam_UPDATE.size(), feats_slam_DELAYED.size(),
+             featsup_MSCKF.size());
+
   // Sort based on track length
   // TODO: we should have better selection logic here (i.e. even feature distribution in the FOV etc..)
   // TODO: right now features that are "lost" are at the front of this vector, while ones at the end are long-tracks
diff --git a/ov_msckf/src/core/VioManagerOptions.h b/ov_msckf/src/core/VioManagerOptions.h
index 0df7d15..6934e26 100644
--- a/ov_msckf/src/core/VioManagerOptions.h
+++ b/ov_msckf/src/core/VioManagerOptions.h
@@ -36,6 +36,7 @@
 #include "init/InertialInitializerOptions.h"
 
 #include "cam/CamEqui.h"
+#include "cam/CamMei.h"
 #include "cam/CamRadtan.h"
 #include "feat/FeatureInitializerOptions.h"
 #include "track/TrackBase.h"
@@ -269,7 +270,12 @@ struct VioManagerOptions {
         cam_eigen.block(4, 0, 3, 1) = -T_CtoI.block(0, 0, 3, 3).transpose() * T_CtoI.block(0, 3, 3, 1);
 
         // Create intrinsics model
-        if (dist_model == "equidistant") {
+        if (dist_model == "mei") {
+          double cam_xi = 0.0;
+          parser->parse_external("relative_config_imucam", "cam" + std::to_string(i), "xi", cam_xi);
+          camera_intrinsics.insert({i, std::make_shared<ov_core::CamMei>(matrix_wh.at(0), matrix_wh.at(1), cam_xi)});
+          camera_intrinsics.at(i)->set_value(cam_calib);
+        } else if (dist_model == "equidistant") {
           camera_intrinsics.insert({i, std::make_shared<ov_core::CamEqui>(matrix_wh.at(0), matrix_wh.at(1))});
           camera_intrinsics.at(i)->set_value(cam_calib);
         } else {
diff --git a/ov_msckf/src/update/UpdaterMSCKF.cpp b/ov_msckf/src/update/UpdaterMSCKF.cpp
index 9a00ade..3c0167f 100644
--- a/ov_msckf/src/update/UpdaterMSCKF.cpp
+++ b/ov_msckf/src/update/UpdaterMSCKF.cpp
@@ -39,6 +39,14 @@ using namespace ov_core;
 using namespace ov_type;
 using namespace ov_msckf;
 
+namespace ov_msckf {
+double g_vision_noise_mult = 1.0;
+// project-local: per-update reprojection residual stats for logging
+double g_last_reproj_rms = -1.0;
+int g_last_reproj_n = 0;
+}
+
+
 UpdaterMSCKF::UpdaterMSCKF(UpdaterOptions &options, ov_core::FeatureInitializerOptions &feat_init_options) : _options(options) {
 
   // Save our raw pixel noise squared
@@ -61,6 +69,9 @@ void UpdaterMSCKF::update(std::shared_ptr<State> state, std::vector<std::shared_
   if (feature_vec.empty())
     return;
 
+  size_t fs_in = feature_vec.size();
+  size_t fs_meas_rej = 0, fs_tri_fail = 0, fs_refine_fail = 0, fs_chi2_rej = 0;
+
   // Start timing
   boost::posix_time::ptime rT0, rT1, rT2, rT3, rT4, rT5;
   rT0 = boost::posix_time::microsec_clock::local_time();
@@ -87,6 +98,7 @@ void UpdaterMSCKF::update(std::shared_ptr<State> state, std::vector<std::shared_
     // Remove if we don't have enough
     if (ct_meas < 2) {
       (*it0)->to_delete = true;
+      fs_meas_rej++;
       it0 = feature_vec.erase(it0);
     } else {
       it0++;
@@ -134,6 +146,10 @@ void UpdaterMSCKF::update(std::shared_ptr<State> state, std::vector<std::shared_
 
     // Remove the feature if not a success
     if (!success_tri || !success_refine) {
+      if (!success_tri)
+        fs_tri_fail++;
+      else
+        fs_refine_fail++;
       (*it1)->to_delete = true;
       it1 = feature_vec.erase(it1);
       continue;
@@ -208,7 +224,7 @@ void UpdaterMSCKF::update(std::shared_ptr<State> state, std::vector<std::shared_
     /// Chi2 distance check
     Eigen::MatrixXd P_marg = StateHelper::get_marginal_covariance(state, Hx_order);
     Eigen::MatrixXd S = H_x * P_marg * H_x.transpose();
-    S.diagonal() += _options.sigma_pix_sq * Eigen::VectorXd::Ones(S.rows());
+    S.diagonal() += g_vision_noise_mult * g_vision_noise_mult * _options.sigma_pix_sq * Eigen::VectorXd::Ones(S.rows());
     double chi2 = res.dot(S.llt().solve(res));
 
     // Get our threshold (we precompute up to 500 but handle the case that it is more)
@@ -223,6 +239,7 @@ void UpdaterMSCKF::update(std::shared_ptr<State> state, std::vector<std::shared_
 
     // Check if we should delete or not
     if (chi2 > _options.chi2_multipler * chi2_check) {
+      fs_chi2_rej++;
       (*it2)->to_delete = true;
       it2 = feature_vec.erase(it2);
       // PRINT_DEBUG("featid = %d\n", feat.featid);
@@ -270,6 +287,13 @@ void UpdaterMSCKF::update(std::shared_ptr<State> state, std::vector<std::shared_
   assert(ct_jacob <= max_hx_size);
   res_big.conservativeResize(ct_meas, 1);
   Hx_big.conservativeResize(ct_meas, ct_jacob);
+  if (ct_meas > 0) {
+    g_last_reproj_rms = std::sqrt(res_big.squaredNorm() / (double)ct_meas);
+    g_last_reproj_n = (int)ct_meas / 2;
+  } else {
+    g_last_reproj_rms = -1.0;
+    g_last_reproj_n = 0;
+  }
 
   // 5. Perform measurement compression
   UpdaterHelper::measurement_compress_inplace(Hx_big, res_big);
@@ -279,11 +303,15 @@ void UpdaterMSCKF::update(std::shared_ptr<State> state, std::vector<std::shared_
   rT4 = boost::posix_time::microsec_clock::local_time();
 
   // Our noise is isotropic, so make it here after our compression
-  Eigen::MatrixXd R_big = _options.sigma_pix_sq * Eigen::MatrixXd::Identity(res_big.rows(), res_big.rows());
+  Eigen::MatrixXd R_big =
+      g_vision_noise_mult * g_vision_noise_mult * _options.sigma_pix_sq * Eigen::MatrixXd::Identity(res_big.rows(), res_big.rows());
 
   // 6. With all good features update the state
   StateHelper::EKFUpdate(state, Hx_order_big, Hx_big, res_big, R_big);
-  rT5 = boost::posix_time::microsec_clock::local_time();
+  
+  PRINT_INFO("[FSTATS-UP] in=%zu meas_rej=%zu tri_fail=%zu refine_fail=%zu chi2_rej=%zu used=%zu\n", fs_in, fs_meas_rej, fs_tri_fail,
+             fs_refine_fail, fs_chi2_rej, feature_vec.size());
+rT5 = boost::posix_time::microsec_clock::local_time();
 
   // Debug print timing information
   PRINT_ALL("[MSCKF-UP]: %.4f seconds to clean\n", (rT1 - rT0).total_microseconds() * 1e-6);
diff --git a/ov_msckf/src/update/UpdaterSLAM.cpp b/ov_msckf/src/update/UpdaterSLAM.cpp
index a769d01..9caaba0 100644
--- a/ov_msckf/src/update/UpdaterSLAM.cpp
+++ b/ov_msckf/src/update/UpdaterSLAM.cpp
@@ -40,6 +40,11 @@ using namespace ov_core;
 using namespace ov_type;
 using namespace ov_msckf;
 
+namespace ov_msckf {
+extern double g_vision_noise_mult;
+}
+
+
 UpdaterSLAM::UpdaterSLAM(UpdaterOptions &options_slam, UpdaterOptions &options_aruco, ov_core::FeatureInitializerOptions &feat_init_options)
     : _options_slam(options_slam), _options_aruco(options_aruco) {
 
@@ -223,8 +228,8 @@ void UpdaterSLAM::delayed_init(std::shared_ptr<State> state, std::vector<std::sh
     }
 
     // Measurement noise matrix
-    double sigma_pix_sq =
-        ((int)feat.featid < state->_options.max_aruco_features) ? _options_aruco.sigma_pix_sq : _options_slam.sigma_pix_sq;
+    double sigma_pix_sq = g_vision_noise_mult * g_vision_noise_mult *
+        (((int)feat.featid < state->_options.max_aruco_features) ? _options_aruco.sigma_pix_sq : _options_slam.sigma_pix_sq);
     Eigen::MatrixXd R = sigma_pix_sq * Eigen::MatrixXd::Identity(res.rows(), res.rows());
 
     // Try to initialize, delete new pointer if we failed
@@ -389,8 +394,8 @@ void UpdaterSLAM::update(std::shared_ptr<State> state, std::vector<std::shared_p
     // Chi2 distance check
     Eigen::MatrixXd P_marg = StateHelper::get_marginal_covariance(state, Hxf_order);
     Eigen::MatrixXd S = H_xf * P_marg * H_xf.transpose();
-    double sigma_pix_sq =
-        ((int)feat.featid < state->_options.max_aruco_features) ? _options_aruco.sigma_pix_sq : _options_slam.sigma_pix_sq;
+    double sigma_pix_sq = g_vision_noise_mult * g_vision_noise_mult *
+        (((int)feat.featid < state->_options.max_aruco_features) ? _options_aruco.sigma_pix_sq : _options_slam.sigma_pix_sq);
     S.diagonal() += sigma_pix_sq * Eigen::VectorXd::Ones(S.rows());
     double chi2 = res.dot(S.llt().solve(res));
 
